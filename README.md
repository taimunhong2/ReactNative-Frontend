# InternTestFrontend

A search bar to search in a list of movie and show the poster and the details of the movies 



#Requirements
nodejs  version 12.0 and above 
npm - v to check the version of node

#Install
cd to the right directory
npm install @react-navigation/native
npm install @react-navigation/stack
npm install 

#start the server
npm start

